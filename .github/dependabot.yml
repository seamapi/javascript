---
version: 2
updates:
  - package-ecosystem: npm
    directory: '/'
    schedule:
      interval: weekly
    allow:
      - dependency-type: production
    groups:
      seam:
        dependency-type: production
        update-types:
          - patch
          - minor
